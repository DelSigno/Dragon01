package com.dragon.android.util;

import com.dragon.android.data.VertexArray;

public class VertexCreator {
	
	//public static VertexArray createVertexArrayCopies(int xDim, int yDim, int xNum, int yNum){	
	
	//}
	
	
	/*merges the two data points starting with data 1
	public static VertexArray mergeTwoFloatDataToVertexArray(float[] data1, float[] data2,int stride){
		float[] vertexData = new float[data1.length + data2.length];
		
		int offset = 0;
		
		for(int i = 0; i < vertexData.length; i++){
			for(int j = 0; j < stride; j++){
				vertexData[offset++] = data1[i+j];
			}
			for(int j = 0; j < stride; j++){
				vertexData[offset++] = data2[i+j];
			}
		}
		
	}*/
}
