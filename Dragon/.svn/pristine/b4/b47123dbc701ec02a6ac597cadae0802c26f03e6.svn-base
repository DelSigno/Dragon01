package com.dragon.android.util;

import static android.opengl.Matrix.multiplyMM;
import static android.opengl.Matrix.setIdentityM;
import static android.opengl.Matrix.translateM;

public class MatrixHandler {
	
	private static final String TAG = "MatrixHandler" ;
	
	private static void normalize(){
		
	}
	
	public static float[] postitionObject(float x, float y, float z) {
		return null;

		//translationMatrix = projectionMatrix;
		//translateM(translationMatrix, 0, x, y, z);
		//setIdentityM(modelMatrix, 0);
		//translateM(modelMatrix, 0, x, y, z);
		//multiplyMM(modelViewProjectionMatrix, 0, projectionMatrix, 0, modelMatrix, 0);
	}
	
}
